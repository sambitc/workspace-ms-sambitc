spring:
  datasource:
    platform: mysql
    url: jdbc:mysql://${SERVICE_REGISTRY_HOST}:3306/music
    username: root
    password: mindfire
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
